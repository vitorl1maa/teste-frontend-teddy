import { FormattedValuePipe } from './formatted-value.pipe';

describe('FormattedValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedValuePipe();
    expect(pipe).toBeTruthy();
  });
});
